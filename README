pyPaint is a 2d drawing library with gtk support.

Install:
  # cd ext/aggdraw
  # sudo python setup.py install
  # cd ../..
  # sudo python setup.py install

  
Aggdraw modifications:
  # support for quadratic and cubic beziers.
